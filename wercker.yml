box: takke/android-wercke
steps:
  - wercker/android-sdk-update@0.0.14r
build:
  steps:
    - android-sdk-update:
      filter: platform-tools,build-tools-22.0.1,extra-android-m2repository,extra-google-m2repository
    - script:
      name: run gradle
      code: |
        gradle --full-stacktrace -q --project-cache-dir=$WERCKER_CACHE_DIR build
